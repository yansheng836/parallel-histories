# 贡献指南

感谢你有兴趣为「中外历史对照时间轴」出力。这个项目结构极简——**一个 `index.html` 就是全部**，所以贡献流程也很简单。

[English](CONTRIBUTING.en.md) | **简体中文**

---

## 目录

- [可以贡献什么](#可以贡献什么)
- [开发环境](#开发环境)
- [内容规范](#内容规范)
- [HTML 结构参考](#html-结构参考)
- [提交 Pull Request](#提交-pull-request)
- [提交信息格式](#提交信息格式)
- [内容准确性要求](#内容准确性要求)
- [行为准则](#行为准则)

---

## 可以贡献什么

按价值从高到低：

| 类型 | 说明 | 难度 |
|------|------|------|
| **史实纠错** | 年份、人物、事件描述有误 —— 最有价值的贡献 | ⭐ |
| **补充细节** | 已有条目补充关键人物、具体年份、影响 | ⭐ |
| **补充事件** | 新增时间刻度或对照条目 | ⭐⭐ |
| **补充中外联动** | 找出两个文明真正交汇的节点 | ⭐⭐ |
| **样式／交互改进** | 响应式、可访问性、打印样式、性能 | ⭐⭐⭐ |
| **翻译** | 英文版内容同步、其他语言版本 | ⭐⭐ |

**不需要**做的事情：改构建配置（没有构建）、加依赖（刻意保持零依赖）、引入框架（原生 HTML/CSS/JS 是设计决策，不是技术债）。

## 开发环境

不需要任何环境。

```bash
git clone git@github.com:yansheng836/parallel-histories.git
cd parallel-histories

# 起个本地服务，改完刷新即可
python -m http.server 8000
```

或者直接用浏览器打开 `index.html`。改完保存 → 刷新页面 → 看效果。

浏览器建议用 Chrome / Edge 打开 DevTools 的设备模拟（`Ctrl+Shift+M`），切到 375px 宽度检查手机端布局。

## 内容规范

### 年份写法

页面是中文语境，统一用中文表述：

| 场景 | 写法 |
|------|------|
| 公元前 | `前221年`、`约前2070年` |
| 公元后 | `618年`、`1949年` |
| 年份区间 | `（618—907）` 用中文破折号 `—`，不加"年" |
| 精确日期 | `1911.10.10`、`1949.10.1` |
| 世纪 | `18世纪中叶`、`公元前3世纪` |
| 模糊年代 | `约前2000—前1200`、`19世纪60—90年代` |

### 卡片结构

每张卡片由三部分组成，按顺序：

```html
<div class="card cn"><i class="node"></i>
  <h3>事件标题 <span class="y">（起止年份）</span></h3>
  <div class="fig">关键人物、关键细节</div>
  一句话概括这件事的历史意义。</div>
```

- `<h3>` 里的 `<span class="y">` 放年份，会被淡化显示
- `<div class="fig">` 放**人物和细节**（人名建议用 `<b>` 加粗）
- 卡片正文最后一段放**意义概括**，不用标签包裹
- 一个人物如果第一次出现，格式用 `<b>汉武帝刘彻</b>（前141-87在位）`

### 中外联动的判断

`link-note` 卡片**只在两个文明真正发生影响关系时使用**，不要拿它来表示"同一时期"。

✅ 合适的例子：

- 张骞通西域 → 丝绸之路 → 中国丝绸抵达罗马
- 怛罗斯之战(751) → 造纸术西传
- 工业革命 → 鸦片走私 → 虎门销烟 → 鸦片战争
- 十月革命 → 马克思主义传入 → 中共成立

❌ 不合适的例子：

- "唐朝时阿拉伯帝国也在崛起" —— 只是同期，没有直接关联
- "宋朝时欧洲在十字军东征" —— 平行发生，不是联动

## HTML 结构参考

### 时间刻度

```html
<div class="tick" id="t-tang"><span class="year">618年</span><span class="era-tag l">唐朝建立</span></div>
```

- `id` 命名约定：`t-` + 简短标识（`t-tang`、`t-1840`、`t-sui`）
- `.era-tag.l` 标签显示在左侧，`.era-tag.r` 显示在右侧（仅在桌面端可见，手机端自动显示为内联文字）

### 左右对照行

```html
<div class="row">
  <div class="card cn"><i class="node"></i>……</div>
  <div class="card west"><i class="node"></i>……</div>
</div>
```

一个 `.row` 就是一组左右对照。其中一侧没有对应内容时，可以只写一个卡片，布局会自动处理。多个 `.row` 可以共用一个 `.tick`（表示同一时期的多组事件）。

### 分期色带

```html
<div class="era-cn" data-from="#t-qin" data-to="#t-sanguo">秦汉·大一统</div>
<div class="era-west" data-from="#t-sui" data-to="#t-ming">中世纪·基督教文明</div>
```

- `data-to` 可以省略，表示延伸到时间轴末尾
- `data-from` / `data-to` **必须指向存在的 `id`**，否则脚本会跳过该色带
- `.era-cn` 贴左侧，`.era-west` 贴右侧

### 中外联动卡片

```html
<div class="link-note"><div class="inner">
  ⚡ <b>联动主题</b>：因 → 果 → 影响。
</div></div>
```

放在两个 `.row` 之间，独立成行，居中显示。

## 提交 Pull Request

```bash
# 1. 从最新 main 切出分支
git checkout main && git pull
git checkout -b fix/tang-dynasty-date

# 2. 改动 index.html

# 3. 本地自测（重要）
#    - 桌面端：1280px 宽度，检查左右卡片是否对齐、色带是否贴合刻度
#    - 手机端：375px 宽度，检查是否变成单列、卡片箭头是否正常
#    - 打开 DevTools Console，确认没有报错

# 4. 提交并推送
git add index.html
git commit -m "fix: 修正唐朝建立年份"
git push -u origin fix/tang-dynasty-date

# 5. 在 GitHub 上创建 PR，填写 .github/PULL_REQUEST_TEMPLATE.md 里的检查项
```

### PR 检查清单

提交前请确认：

- [ ] 只改了必要的内容，没有顺手格式化整个文件（会让 diff 无法阅读）
- [ ] 在桌面端 1280px 下检查过布局
- [ ] 在手机端 375px 下检查过布局
- [ ] Console 无报错
- [ ] 新增的 `data-from` / `data-to` 指向了存在的 `id`
- [ ] 史实来源可靠，不是凭印象写的（见下节）
- [ ] README 预览图如果过时了，一并更新（可选）

## 提交信息格式

使用 `type: 中文描述`：

```
feat: 补充明朝郑和下西洋细节
fix: 修正安史之乱结束年份
docs: 更新贡献指南的年份写法说明
style: 优化手机端卡片间距
refactor: 提取时间轴定位逻辑为独立函数
chore: 添加 GitHub Pages 部署工作流
```

| type | 用途 |
|------|------|
| `feat` | 新增历史事件、时间刻度、联动卡片 |
| `fix` | 修正史实错误、布局 bug |
| `docs` | 只改文档 |
| `style` | 只改样式，不影响内容 |
| `refactor` | 重构代码，不改变行为 |
| `test` | 测试相关 |
| `ci` | CI / 部署配置 |
| `chore` | 其他杂项 |

## 内容准确性要求

这一节请认真读，是项目最看重的部分。

1. **优先采用通说**。同一事件有多种说法时，采用主流教材和历史学界的通行观点，并在卡片里注明存在争议（如果差异较大）。
2. **不要凭印象写年份**。不确定的话，要么去查证，要么写"约"。
3. **区分"事件发生"和"制度确立"**，例如隋朝 581 年建立、589 年灭陈完成统一，这两个年份不是一回事。
4. **注意朝代纪年与公元纪年的换算**。中国早期年代（夏商周）采用夏商周断代工程成果，与部分海外研究存在差异，本项目采用前者的通行数据。
5. **近现代史部分保持客观**。表述以中国现行中学历史教材为基础，不用情绪化措辞，不做超出史实范围的评价。
6. **人物生卒年不确定时**，只写在位年份或不写，例如 `（前141-87在位）`。

如果你提交的是**纠错**，请在 PR 描述里附上依据（教材名称、权威著作、或可靠网页来源），这样 review 会快很多。

## 行为准则

- 讨论历史问题请保持**学术态度**，尊重不同观点，不进行政治攻击和人身攻击
- 历史话题容易引发争论，如果某处表述存在长期争议，维护者可能会选择**中性表述**或**注明多种说法**，这不代表立场
- 不接受任何形式的歧视性言论
- 违规内容会被直接关闭，重复违规会被 block

---

<div align="center">

有疑问？[提 Issue](https://github.com/yansheng836/parallel-histories/issues) 或者直接在 PR 里问。

</div>
